name: CI

on:
  push:
    branches: [main]
  pull_request:
    # Release pull requests change only these two files. Filtering here rather
    # than skipping the job matters: a bot-authored pull request needs workflow
    # approval before any job runs, so a job-level condition never gets a chance
    # to evaluate and the run ends up marked failed.
    paths-ignore:
      - 'CHANGELOG.md'
      - '.release-please-manifest.json'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.25'
          check-latest: true

      - name: Formatting
        run: |
          unformatted=$(gofmt -l .)
          if [ -n "$unformatted" ]; then
            echo "These files need gofmt:"; echo "$unformatted"; exit 1
          fi

      - name: Vet
        run: go vet ./...

      - name: Test
        run: go test ./...

      - name: Cross-compile every target
        run: |
          for t in windows/amd64 windows/arm64 darwin/amd64 darwin/arm64 linux/amd64 linux/arm64; do
            GOOS=${t%/*} GOARCH=${t#*/} go build -o /dev/null . || exit 1
            echo "  ok $t"
          done
